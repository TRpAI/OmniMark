# Cloudflare Pages 基础配置文件
# 注意：本项目已全面适配 Cloudflare 网页面板进行可视化绑定（不需要在 toml 里写死 ID，避免格式校验错误）
name = "omnimark-navigation"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

[vars]
ENVIRONMENT = "production"
